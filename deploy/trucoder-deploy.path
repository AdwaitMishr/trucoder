[Unit]
Description=Trigger TruCoder deploy when the webhook trigger file changes

# Edit the path to match your checkout (the file is created on first boot).
[Path]
PathChanged=/opt/trucoder/.deploy-trigger

[Install]
WantedBy=multi-user.target
